# Client Configuration

# Optional: API URL for the backend (defaults to /api in production, http://localhost:3000/api in dev)
# VITE_API_URL=https://your-api-server.com/api

# Optional: Vercel protection bypass token (only needed if using Vercel authentication protection)
# VITE_VERCEL_PROTECTION_BYPASS=your-bypass-token-here
